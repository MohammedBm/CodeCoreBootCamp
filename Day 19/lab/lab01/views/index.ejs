<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>NOT error</title>
</head>

<body>


  <% if (preLang == "en") { %>
    <h1>Hello, <%= cookieName %>!</h1>
    <% } else if (preLang == "fr") { %>
      <h1>Bonjur, <%= cookieName %>!</h1>
    <% } else if(preLang == "sp") { %>
      <h1>Hola, <%= cookieName%> !</h1>
    <% }else{ %>
      <h1>Who are you? <%= cookieName %>!</h1>

      <form class="" action="/cookieName" method="POST">
        <div>
          <label for="">Enter your name: </label>
          <input type="text" name="cookieName" />
        </div>

        <div>
          <label>Select your prefered language</label>
          <select name = "preLang">
            <option value="en">English</option>
            <option value="fr">French</option>
            <option value="sp">Spanish</option>
          </select>
        </div>


        <div>
          <input type="submit" value="Submit" />
        </div>

      </form>



      <% } %>









</body>

</html>
